---
interface Props {
  name: string;
  class?: string;
}
const { name, class: className } = Astro.props;
---

<svg class={`i ${className ?? ''}`} aria-hidden="true">
  <use href={`#i-${name}`}></use>
</svg>
